// Template. Copy to secrets.h and fill in -- secrets.h is gitignored.
//
//   cp include/secrets.example.h include/secrets.h
//
// The C3 has no 5GHz radio, so the network must be 2.4GHz.
#pragma once

#define WIFI_SSID ""
#define WIFI_PASS ""
