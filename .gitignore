.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Go relay build output
server/relay

# Real WiFi credentials -- never commit these (see include/secrets.example.h)
include/secrets.h

# nginx runtime files for the dashboard
dashboard/tmp/
dashboard/nginx.pid

# Written by run.sh: the relay admin token, and the compose env holding it
.admin-token
deploy/grafana/.env
